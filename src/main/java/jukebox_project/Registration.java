package jukebox_project;

public class Registration extends Exception
{
   //constructor which takes a String message as an argument
    public Registration(String message) {
        // and passes it to the super class constructor using the "super" keyword
        super(message);
    }

}
